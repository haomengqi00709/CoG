# Git Commands to Run

Run these commands to push your changes:

```bash
cd /Users/jasonhao/Desktop/CoG

# Check status
git status

# Add all changes
git add .

# Commit with message
git commit -m "docs: Organize documentation with numbered files and merge GitHub setup guide"

# Push to GitHub
git push origin main
```

